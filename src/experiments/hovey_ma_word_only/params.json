{
    "learning_rate": 1e-3,
    "batch_size": 10,
    "num_epochs": 50,

    "lstm_hidden_dim": 200,
    "embedding_size": 100,
    "dropout_rate": 0.5,

    "embed_types": ["word"],

    "save_summary_steps": 100
}
